[project]
name = "dir-compat"
version = "0.1.0"
description = "Directory compatibility checker"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
dir-compat = "dir_compat.main:main"

[tool.uv]
package = true